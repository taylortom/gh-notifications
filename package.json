{
  "name": "gh-notifications",
  "version": "1.0.0",
  "type": "module",
  "engines": {
    "node": ">=20.6.0"
  },
  "scripts": {
    "start": "node --env-file=.env bin/start.js",
    "dev": "node --watch --env-file=.env bin/start.js"
  }
}
